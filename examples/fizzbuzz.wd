fizzbuzz: func(max) =>
    count => 0
    msg => ""
    while count <= max, count => count + 1:
        msg => count
        if count % 3 == 0:
            msg => "Fizz"
        endif
        if count % 5 == 0:
            msg => "Buzz"
        endif
        if count % 3 == 0 and count % 5 == 0:
            msg => "FizzBuzz"
        endif
        !(printLine msg)
    endwl
endfn

!(print "Enter a max number: ")
!(fizzbuzz !(toNum !input))

