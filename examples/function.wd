add: func(x, y, z) =>
    <- x + y + z
endfn

generate_add: func(num) =>
    new: func(x) =>
        <- x + num
    endfn
    <- new
endfn

main: func() =>
    add_5 => !(generate_add 5)
    add_8 => !(generate_add 8)
    
    count => 0
    while count < 20 =>
        if count % 2 == 0 =>
            !(print true)
            !(print !(add_5 count))
        else =>
            !(print false)
            !(print !(add_8 count))
        endif
        count => count + 1
    endwl
endfn

!main
