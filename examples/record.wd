BinOp: record =>
    right => null
    operator => null
    left => null
endrc

a: func(x, y) =>
    s => 0
    <- x
endfn

Number: record =>
    value => null
endrc

expr => !{BinOp
    !{Number 6}
    "+"
    !{BinOp
        !{Number 8}
        "*"
        !{Number 10}
    }
}

!(printLine expr)
